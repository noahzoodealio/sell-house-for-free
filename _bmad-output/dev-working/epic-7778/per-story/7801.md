# E2-S5 / 7801 — per-story summary

**Outcome:** closed — 14/15 ACs ✅, AC 15 deferred to S11 (validation sweep).
**Commits:** `184e7d1` add JSON-LD helpers + <JsonLd> renderer (schema-dts-typed)
**Net change:** +2 files, +233 lines. No deps change (schema-dts installed by S1).
**Deviations:** FaqEntryShape + CityEntryShape defined locally with TODO markers (per AC #4 allowance); swap to `@/content/faq/entries` + `@/content/cities/registry` when S4 + S10 land.
**Deferred:** AC #15 smoke-page validation — S11 owns the actual JSON-LD sweep across pages that emit schema (S6-S10).
**ADO:** `New → In Development → Code Review`.
