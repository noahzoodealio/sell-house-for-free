# E2-S2 / 7798 — per-story summary

**Outcome:** closed — 16/17 ACs ✅, AC 15 (smoke route) deferred to S6+ real compositions.
**Commits:** single commit on shared branch (message "e2-s2(7798): marketing components wave 1…")
**Net change:** +6 files under `src/components/marketing/` (hero, pillar-grid, trust-bar, cta-section, page-header, cta-link helper). +1 token (`--color-brand-subtle` in `globals.css`).
**Key decisions:** CtaLink helper wraps `next/link` with primary/secondary/quiet variants; the E1 `<Button>` primitive is form-submit-shaped so we built an anchor-based CTA separately rather than hack Button into a Link. Full-card link pattern on PillarGrid uses `md:before:absolute md:before:inset-0` on heading's Link rather than an overlay sibling link.
**ADO:** `New → In Development → Code Review`.
