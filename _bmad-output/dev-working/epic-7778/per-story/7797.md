# E2-S1 / 7797 — per-story summary (epic-level)

**Outcome:** closed — all 15 ACs green, flipped to ADO `Code Review`.
**Commits on shared branch `feature/e2-core-marketing-pages-7778`:**
- `fd2b436` wire @next/mdx into build (deps + next.config)
- `b7b8d55` add mdx-components.tsx + .prose-custom + surface-muted token
- `da7bf76` add mdx-smoke route to validate MDX pipeline end-to-end
- `9201488` remove mdx-smoke after MDX pipeline validation

**Net change:** +4 runtime deps, +1 devDep, 1 new file (`src/mdx-components.tsx`), 2 edits (`next.config.ts`, `src/app/globals.css`). No new routes in final state.

**Deviations:** (1) `mdx-smoke` not `__mdx-smoke` — Next 16 `_`-prefix is private-folder convention; (2) `.prose-custom` uses `rem` literals not `--space-*` tokens — E1 didn't ship that scale.

**Unit-testing outcome:** skipped — pure infrastructure, no business logic. Mappers verified transitively via smoke route prerender before deletion.

**Code-review outcome:** self-review comprehensive with AC matrix + pattern compliance check against E1 idioms (ref-as-prop, no `cn()`/`forwardRef`, no `"use client"`, Turbopack options `{}`). No separate reviewer pass.

**Carry-forward to later stories:**
- E2-S3 owns `<ProseContainer>` that applies `className="prose-custom"`.
- E2-S5 consumes `schema-dts` (already installed).
- E2-S7, S9 can now ship real `.mdx` routes.
