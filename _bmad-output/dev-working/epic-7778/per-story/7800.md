---
story-id: 7800
code: E2-S4
outcome: closed
closed-at: 2026-04-20
review-verdict: self-pass
unit-testing: skipped-no-business-logic
commits: [e154e6a]
ado-final-state: Code Review
---

# E2-S4 (7800) — FAQ system — closeout

**15/16 ACs green.** AC 11 (hover hash-link icon on `<summary>`) deferred — the AC itself flagged it as "Not blocking if deferred; nice-to-have."

## Shipped
- `src/content/faq/entries.ts` — `FaqEntry`, `FaqCategory`, `FAQ_CATEGORIES`, `FAQ_CATEGORY_LABELS`; 12 seed entries across 6 categories; 3 flagged `skepticFirst` (what's-the-catch, do-you-sell-my-info, who-is-my-pm).
- `src/components/marketing/faq-item.tsx` — native `<details>/<summary>`, webkit + modern marker hidden via Tailwind arbitrary variants, chevron SVG rotates on `group-open`, answer split on `\n\n`.
- `src/components/marketing/faq.tsx` — category grouping in fixed order, skeptic-first sort, `category` prop for home-excerpt mode, optional `title`.
- `src/components/marketing/faq-hash-target.tsx` — 12-line `'use client'` helper; opens matching `<details>` on mount.
- `src/app/(marketing)/faq/page.tsx` — `PageHeader` + `FAQ` + `JsonLd(faqPageSchema(entries))` + `FaqHashTarget`.

## Collateral
- `src/lib/schema.ts` — closed `TODO(E2-S4)`; `faqPageSchema()` now consumes `ReadonlyArray<FaqEntry>` from the canonical source.
- `src/lib/routes.ts` — appended `/faq` (sitemap picks it up).

## Unit testing
Skipped. All logic is either type-level or declarative rendering (map + sort by `skepticFirst`). No business rules to test in isolation — the sort behavior is exercised visually by the `/faq` render.

## Hand-offs
- **S6 (7802 — home):** can render `<FAQ entries={entries} category="free-and-fair" title="Common questions" />` for the home excerpt without any FAQ component changes.
- **S8 (7804 — pillar pages):** can render `<FAQ entries={entries.filter(e => e.relatedPillar === 'cash-plus-repairs')} />`… or use the `category` prop if we add a `pillar` filter dim later.
- **S11 (7807 — anti-broker audit):** each entry has `lastReviewed: "2026-04-20"`. The `do-you-sell-my-info` and `licensed-az-broker` answers are the two audit hooks.

## Build + lint
- `tsc --noEmit`: clean.
- `next build`: 12/12 static pages generated; `/faq` is `○ (Static)`.
- `eslint`: 0 errors; 1 pre-existing warning in `pillar-hero.tsx` (7799 leftover), not in 7800 scope.
