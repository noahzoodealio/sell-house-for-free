---
description: 'Search API endpoints across services'
disable-model-invocation: true
---

Activate the `zoo-core-find-endpoint` skill. Follow its SKILL.md at `{project-root}/.claude/skills/zoo-core-find-endpoint/SKILL.md` — including the preflight check (which may invoke `zoo-core-onboard` first if required files are missing).
