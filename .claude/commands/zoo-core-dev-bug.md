---
description: 'Fix an ADO Bug with root-cause tracing'
disable-model-invocation: true
---

Activate the `zoo-core-dev-bug` skill. Follow its SKILL.md at `{project-root}/.claude/skills/zoo-core-dev-bug/SKILL.md` — including the preflight check (which may invoke `zoo-core-onboard` first if required files are missing).
