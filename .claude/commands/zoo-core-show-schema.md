---
description: 'Look up an entity schema across services'
disable-model-invocation: true
---

Activate the `zoo-core-show-schema` skill. Follow its SKILL.md at `{project-root}/.claude/skills/zoo-core-show-schema/SKILL.md` — including the preflight check (which may invoke `zoo-core-onboard` first if required files are missing).
