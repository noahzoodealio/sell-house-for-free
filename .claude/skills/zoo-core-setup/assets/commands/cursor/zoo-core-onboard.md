---
description: 'Bootstrap the zoo-core workspace — resolve repo paths + scaffold local memory'
---

Activate the `zoo-core-onboard` skill. Follow its SKILL.md at `{project-root}/.claude/skills/zoo-core-onboard/SKILL.md` — including the preflight check (which may invoke `zoo-core-onboard` first if required files are missing).
