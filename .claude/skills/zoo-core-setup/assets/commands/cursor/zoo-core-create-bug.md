---
description: 'File an ADO Bug with data-flow tracing'
---

Activate the `zoo-core-create-bug` skill. Follow its SKILL.md at `{project-root}/.claude/skills/zoo-core-create-bug/SKILL.md` — including the preflight check (which may invoke `zoo-core-onboard` first if required files are missing).
