---
description: 'Generate ADO Test Cases for human QA'
---

Activate the `zoo-core-create-tests` skill. Follow its SKILL.md at `{project-root}/.claude/skills/zoo-core-create-tests/SKILL.md` — including the preflight check (which may invoke `zoo-core-onboard` first if required files are missing).
