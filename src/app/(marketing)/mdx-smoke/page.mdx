export const metadata = {
  title: "MDX smoke test",
  robots: { index: false, follow: false },
};

# MDX smoke

Pipeline validation for `@next/mdx`. Removed before E2-S1 close-out.

## Type ramp sanity

A paragraph with **bold text**, _italic text_, and inline `code`.
[Internal link](/) should prefetch via `next/link`. [External link](https://nextjs.org)
should open in a new tab with the announce-only aria-label suffix.

### Lists

- one
- two
- three

1. alpha
2. beta
3. gamma

> A blockquote should inherit the brand-accent border.

---
