import { buildMetadata } from "@/lib/seo";
import { LINKS } from "@/lib/links";
import { SITE } from "@/lib/site";
import { webPageSchema } from "@/lib/schema";
import { PageHeader } from "@/components/marketing/page-header";
import { ProseContainer } from "@/components/marketing/prose-container";
import { RevenueTable } from "@/components/marketing/revenue-table";
import { CTASection } from "@/components/marketing/cta-section";
import { JsonLd } from "@/components/marketing/json-ld";
import { streams } from "@/content/revenue/streams";

export const metadata = buildMetadata({
  title: "Why it’s free",
  description:
    "How we make money without charging home sellers. Real revenue streams, real disclosure, no asterisks.",
  path: LINKS.whyItsFree,
});

<PageHeader
  eyebrow="Why it’s free"
  heading="How we make money without charging you"
  subcopy="A short, honest accounting of every revenue stream that funds Sell Your House Free."
/>

<ProseContainer>

The short answer: every dollar that funds this service comes from the buyer
side of a closed transaction, never from you. If a deal does not close, no one
owes us anything — including you.

Below is the full list of how we earn. There is no “asterisk” tier; if a
revenue stream isn’t in this table, we don’t earn it.

</ProseContainer>

<RevenueTable streams={streams} />

<ProseContainer>

## What we do *not* take

- We don’t charge a seller-side listing commission.
- We don’t sell your contact information to outside agents or investors.
- We don’t take referral fees from third-party services.
- We don’t run third-party tracking pixels — only Vercel’s first-party analytics, in production only.

If you want the deeper detail on PM accountability, see [How it works]({LINKS.howItWorks}).
For the brand context, see [About]({LINKS.about}).

</ProseContainer>

<CTASection
  heading="Ready to see real numbers?"
  subcopy="Your PM will model every path against your home — listing, cash, Cash+, Renovation-Only — with real numbers, before you commit."
  primaryCta={{ label: "Get my free intake", href: LINKS.getStarted }}
  secondaryCta={{ label: "Common questions", href: LINKS.faq }}
/>

<JsonLd
  id="ld-webpage"
  data={webPageSchema({
    name: "Why it’s free",
    description:
      "How Sell Your House Free earns: every revenue stream, with who pays and when.",
    url: `${SITE.url}${LINKS.whyItsFree}`,
  })}
/>
